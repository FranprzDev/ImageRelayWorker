# Windows MSI installer

The MSI is built in GitHub Actions with WiX Toolset 4. It installs the worker as an automatic Windows service and collects the required configuration through the installer UI.
